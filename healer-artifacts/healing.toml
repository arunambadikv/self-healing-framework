# Canonical project config (written to healer-artifacts/healing.toml by healing-init).
# Prefer this path over a root healing.toml; [tool.healing] in pyproject.toml also works.
pages_dir = "pages"
tests_dir = "tests"
data_dir = "data"
artifacts_dir = "healer-artifacts"
apply_roots = ["pages"]
skills_dir = ".cursor/skills"
mcp_config = ".cursor/mcp.json"
auth_dir = "healer-artifacts/auth"

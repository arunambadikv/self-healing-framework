---
description: Use Playwright MCP to repair Page Object locators in pages/*.py from failure reports and pomhealer-queue patches.
globs:
  - pages/**
  - pomhealer-artifacts/failures/**
  - pomhealer-artifacts/pomhealer-queue/**
  - pomhealer-artifacts/architecture/**
alwaysApply: false
---

# Playwright MCP Locator Repair (POM)

1. Use the connected **Playwright MCP** server.
2. Read failure: `pomhealer-artifacts/failures/F-<id>.md`
3. Follow **`.cursor/skills/playwright-locator-repair/SKILL.md`** for diagnosis and proposal JSON.
4. Human apply: **`.cursor/skills/pomhealer-review/SKILL.md`** + `python -m pomhealer.review --patch P-<id> --decision heal`

## MCP sequence

1. `browser_navigate` — `base_url` from failure JSON
2. `browser_snapshot` — find element for architecture ref

## Rules

- Update **`pages/*.py`** only when proposing or applying.
- Write `pomhealer-artifacts/pomhealer-queue/patches/P-<id>.json` with `architecture_updates[]`.
- Do not weaken assertions or skip tests.
